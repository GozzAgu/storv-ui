/**
 * Shared layout tokens for the header profile menu dropdown.
 */
export function useDashboardProfileMenuChrome() {
  const triggerClass = 'dash-profile-trigger'
  const triggerCompactClass = 'dash-profile-trigger--compact'
  const triggerDefaultClass = 'dash-profile-trigger--default'

  const avatarClass = 'dash-profile-avatar'

  const panelClass = 'dash-profile-panel'

  const panelHeaderClass = 'dash-profile-panel__header'

  const panelHeaderNameClass = 'dash-profile-panel__name'

  const panelHeaderEmailClass = 'dash-profile-panel__email'

  const metaBadgeClass = 'dash-profile-badge'

  const roleBadgeClass = 'dash-profile-badge dash-profile-badge--role'

  const menuSectionLabelClass = 'dash-profile-section-label'

  const menuSectionClass = 'dash-profile-section'

  const menuRowClass = 'dash-profile-row'

  const menuRowInactiveClass = 'dash-profile-row--inactive'

  const menuRowActiveClass = 'dash-profile-row--active'

  const menuIconWrapClass = 'dash-profile-row__icon'

  const menuIconWrapActiveClass = ''

  const menuIconClass = ''

  const menuBadgeClass = 'dash-profile-badge-count'

  const menuFooterClass = 'dash-profile-footer'

  const signOutRowClass = 'dash-profile-sign-out'

  const signOutIconWrapClass = 'dash-profile-sign-out__icon'

  return {
    triggerClass,
    triggerCompactClass,
    triggerDefaultClass,
    avatarClass,
    panelClass,
    panelHeaderClass,
    panelHeaderNameClass,
    panelHeaderEmailClass,
    metaBadgeClass,
    roleBadgeClass,
    menuSectionLabelClass,
    menuSectionClass,
    menuRowClass,
    menuRowInactiveClass,
    menuRowActiveClass,
    menuIconWrapClass,
    menuIconWrapActiveClass,
    menuIconClass,
    menuBadgeClass,
    menuFooterClass,
    signOutRowClass,
    signOutIconWrapClass,
  }
}
